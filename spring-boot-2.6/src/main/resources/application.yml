management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    env:
      post:
        enabled: true
    health:
      show-details: ALWAYS

logging:
  file:
    name: "logs.txt"

app:
  spring:
    boot:
      version: "@project.version@"